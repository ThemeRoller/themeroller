/*
 * ThemeRoller Model JavaScript file
 * @requires jQuery (external)
 *
 * ** ADD LICENSE HERE **
 */
(function( exports, $, undefined ) {

/**
 * class Model
 *
 * ThemeRoller Model, simple model abstraction, containing the interactive data
 * and logic surrounding it: conversions, validations, computed properties, and
 * access control.
 *
 * Events:
 * - 
 *
 */

/**
 * new Model( )
 * - 
 *
 */
function Model( ) {
  // TODO before implementing this, make sure it satisfies Panel requirements.
};

Model.prototype = {
};

/**
 * Export public interface
 */
exports.Model = Model;

}( this, jQuery ));
// vim:ts=2:st=2:sw=2:et:tw=80:
